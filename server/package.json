{
  "name": "server",
    "version": "1.0.0",
    "description": "",
    
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "json-server --watch data.json --port 3002",
        "start:delay": "npm start -- --delay 3000"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
    "json-server": "^0.17.0"
  }
}
